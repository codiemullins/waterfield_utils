machine:
  timezone: America/Chicago
  ruby:
    version: 2.2.6
database:
  override:
    - cd spec/dummy && pwd && bundle env && bundle exec bundle install --verbose && bundle exec rake db:create db:migrate --trace
