machine:
  timezone: America/Chicago
  ruby:
    version: 2.2.6
database:
  override:
    - cd spec/dummy
    - echo $PWD
    - bundle exec bundle install
    - bundle exec rake db:create db:migrate --trace
